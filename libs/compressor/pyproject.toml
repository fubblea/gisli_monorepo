[project]
name = "compressor"
version = "0.1.0"
authors = [
    { name = "fubblea", email = "ajay.arthur@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
